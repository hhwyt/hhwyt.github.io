# 写作格式规范

写中文文章时，遵守以下排版规则：

- 中文汉字与英文字母、数字之间要加空格。例如："AI 时代"、"300 万"、"C++ 和 Linux"、"40 岁一个亿"。
- 引用文字用中文方括号引号「」（或书名号《》用于书名），不要用英文直引号 " "。
- 正文里不要用 Markdown 的双星号（**文字**）表示加粗。公众号、小红书等平台不解析 Markdown，粘贴过去星号会原样显示，变成两个多余的符号。需要强调，靠句子本身的措辞和节奏，不靠标记语法。
- 少用破折号「——」，这个标点很有 AI 生成文本的痕迹。需要停顿、转折或补充说明时，改用句号断句、逗号，或者干脆重组句子结构，不要依赖破折号当万能连接符。
- 跟作者讨论文章结构时，用树形列表展示大纲，不要用标题层级（不要 ## / ### 也不要 1、2、3、4 编号）。每一条要具体、带实际内容（数字、事实、关键句），不要写成抽象的标题概括，方便作者一眼核对内容对不对，而不是只看到一个笼统的小标题。
